import React from "react";
import { Box } from "@mui/material";

const Filters = () =>
{
    return (
        <Box
            sx={{
                flexGrow: 1,
                display: "flex",
                justifyContent: "center",
                alignItems: "center",
            }}
        >
            Admin Filters
        </Box>
    );
};

export default Filters;